%% The TeX code is entering with UTF8
%% character encoding (Linux and MacOS standards)
\usepackage[utf8]{inputenc}

\usepackage[top=1in, bottom=1.25in, left=1.0in, right=1.0in]{geometry}
\usepackage{tabularx}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{xcolor}

% for index
\usepackage{makeidx}

% for appendix
\usepackage{appendix}

% For bibtex
\usepackage{natbib}

% For cute boxes
\usepackage{fancybox}

\setlength{\parindent}{4mm}
\setlength{\parskip}{3mm plus2mm minus2mm} % warning

% Espace insécable (le caractère ~ se traduit par un espace insécable).
%\DeclareUnicodeCharacter{00A0}{~}
% Sur un clavier bépo, on a un accès direct au ×
%\DeclareUnicodeCharacter{00D7}{\ensuremath\times}

\newcommand{\tl}{\ensuremath\textless}
\newcommand{\tg}{\ensuremath\textgreater}

\newcommand{\haato}{\ensuremath\heartsuit}

\newcommand{\ra}{\ensuremath\rightarrow}
\newcommand{\la}{\ensuremath\leftarrow}

\endinput
